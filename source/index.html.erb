---
title: splittingred.com
---
<section>
  <% blog.articles[0...5].each do |article| %>
    <article>
      <h1><a href="<%= article.url %>"><%= article.title %></a></h1>
      <time><%= article.date.strftime('%b %e, %Y') %></time>

      <p class="teaser"><%= article.data.teaser %></p>
    </article>
  <% end %>
</section>